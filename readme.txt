Imapsync:
* download all selected mailbox mails to $XDG_MAIL_DIR/inbox
* move them to Archives/{year}
* delete them

Note:
the folder processing is messy,
if you have no character except for [a-zA-Z0-9_-] as folder name then it's probably safe.
